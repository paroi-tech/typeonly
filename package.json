{
  "name": "@typeonly/checker",
  "version": "0.1.4",
  "description": "Checks data conformity, for example a JSON data.",
  "author": "tomko.team",
  "scripts": {
    "clear": "rimraf 'dist/*' 'bundle-tsd/declarations/*'",
    "tsc": "tsc",
    "tsc:watch": "tsc --watch",
    "bundle-tsd": "node bundle-tsd/bundle-tsd",
    "build": "npm run clear && npm run tsc && npm run bundle-tsd",
    "lint": "tslint -p tsconfig.json -t verbose",
    "test": "jest --watch"
  },
  "dependencies": {
    "@typeonly/reader": "^0.1.4"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^10.14.8",
    "jest": "^24.8.0",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "tslint": "^5.17.0",
    "typeonly": "^0.1.5",
    "typescript": "^3.5.1"
  },
  "main": "dist/api.js",
  "types": "dist/checker.d.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tomko-team/typeonly-checker.git"
  },
  "license": "CC0-1.0",
  "keywords": [
    "json",
    "checker",
    "check",
    "type",
    "interface",
    "typescript"
  ]
}